# Neon Learning Platform - Environment Variables Example
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000

# OpenAI Configuration (Optional)
# If you have an OpenAI API key, add it here for full AI capabilities
# Without this key, the chatbot will use fallback responses
OPENAI_API_KEY=your_openai_api_key_here

# Development Settings
NODE_ENV=development

# API Configuration
API_BASE_URL=http://localhost:3000

# INSTRUCTIONS:
# 1. Copy this file and rename to .env
# 2. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 3. You can get an API key from: https://platform.openai.com/api-keys
# 4. The platform works without an API key, but with limited AI responses
# 5. Keep the .env file private - never commit it to version control